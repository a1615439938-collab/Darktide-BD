# Darktide-BD Handoff / 项目交接

> 更新时间 / Updated: 2026-09-27  
> 仓库 / Repository: `a1615439938-collab/Darktide-BD`  
> 默认分支 / Default branch: `main`

## 1. 产品目标 / Product goal

这是一个面向桌面端与手机端的《Warhammer 40,000: Darktide》中英双语 BD 编辑器。

**当前 UX 核心原则：用户是在“完成一个 BD”，不是在“填写一张数据库表”。**

不要把所有功能重新纵向堆回一张超长页面。低频信息应该折叠，高频路径应该连续，用户必须始终知道当前位于“天赋 / 装备 / BD 信息”的哪个工作区。

## 2. 当前已完成 / Current baseline

### 工作区与导航
- 桌面端：`天赋 / 装备 / BD 信息` 三主视图。
- 手机端：固定底部三栏导航。
- 顶部只保留当前 BD、版本、语言、自动保存状态、职业和天赋点。
- `中文 / 中英 / English` 三种显示模式，默认中文。
- 自动保存显示“正在保存 / 已自动保存 / 保存失败”；不要恢复多余的手动保存按钮。

### BD 管理
- 多套 BD 本地保存：新建、复制、切换、删除、自动保存。
- 新建 / 复制 / 删除收进低干扰操作区，删除不与高频操作并列。
- JSON 导入 / 导出与分享链接。

### 天赋树
- 7 个职业；正式服与 `Depths of the Damned` 预览树。
- 节点、连线、30 点预算、互斥节点。
- 已用 / 剩余点数同时可见。
- 桌面端悬停详情、左键选择 / 取消、右键取消、撤销、缩放、适配。
- 手机端点击先预览，再明确执行选择。
- 锁定节点会计算从当前已选路径到目标节点的连接路径，显示还需要多少点和途经节点，并在树上高亮；互斥时直接说明冲突。
- 详情卡默认只保留影响选择的信息；来源 / 术语等低频内容收进“更多信息”。

### 装备区
- 近战 / 远程连续流程：`武器 → 祝福 1 → 祝福 2 → 词条 1 → 词条 2`。
- 武器支持中文 / 英文搜索，以及全部、推荐、最近使用、武器类别筛选。
- 最近武器历史保存在本机。
- 祝福严格按当前武器过滤。
- 祝福列表直接显示效果与等级摘要；Tooltip 只是补充，不是唯一查看方式。
- 更换武器会自动清除不兼容祝福。
- 配装页显示总完成度；近战、远程、每件珍品均有完成状态。
- 珍品支持复制上一件、第一件复制到全部。
- 手机端珍品 1 默认展开，珍品 2 / 3 默认折叠；珍品外观可选并默认收起。

### 性能
- 当前职业图标立即高优先级加载。
- 其他职业图标在浏览器空闲时后台预加载。
- Data Saver / 极慢网络不强制后台预加载。
- 不要恢复成首屏加载整个 `tree-icons.js`。

## 3. 关键实现文件 / Key files

- `index.html` — 工作区结构、装备卡、弹窗、手机导航。
- `styles.css` — 桌面 / 手机布局、天赋、装备、弹层。
- `app.js` — 交互状态、BD 库、语言模式、装备流程、路径提示、自动保存。
- `tree-core.js` — 当前树数据。
- `tree-icons-*.js` — 职业图标包。
- `blessing-effects.js` — 祝福效果。
- `blessing-tier-values.js` — 祝福等级数值。
- `weapon-blessing-overrides.js` — 新武器家族与祝福合法性覆盖。
- `.github/workflows/self-check.yml` — 手机 / 桌面交互、弹层几何、天赋页和装备页截图。
- `.github/workflows/text-audit.yml` — 文本与来源审计。
- `.github/workflows/pages.yml` — Pages 部署。
- `.github/workflows/refresh-trees.yml` — 天赋树刷新。

## 4. 不要回退的 UX 决策 / Do not regress

1. **不要恢复成长网页。** 天赋、装备、BD 信息保持分区。
2. **不要恢复手动保存按钮。**
3. **不要让默认中文模式重新充满英文副标题。**
4. **不要把装备重新做成一个槽位一个弹窗的机械表单。**
5. **不要把祝福效果藏回 Hover-only。**
6. **不要把三个珍品全部默认展开在手机端。**
7. **不要把来源、审计状态、调试信息重新塞进天赋详情主视图。**
8. 未点亮节点仍可查看详情；点击空白可关闭。
9. 桌面端保留悬停、右键取消、撤销、缩放。
10. 手机端拖动树不得误触节点。
11. 多套 BD 不能退回成只能保存一套。

## 5. 数据与翻译原则 / Data and localization

1. 英文机制文本优先 Fatshark 官方资料、游戏数据或 Games Lantern 当前数据。
2. 天赋中文名称优先维护中的 Darktide 中文术语来源。
3. 武器优先采用游戏官方简体中文译名。
4. 祝福、词条、珍品继续核对官方简体中文。
5. 必须区分“官方简中 / 社区翻译 / 人工补译”；不要把机器翻译冒充官方译名。
6. 新武器家族如果祝福机制已确认但 I–IV 数值表尚未覆盖，不得套用旧武器家族数值。

## 6. 自动化与视觉回归 / QA

`Mobile UI self-check` 同时检查行为并生成：
- 手机天赋页截图；
- 桌面天赋页截图；
- 手机装备页截图；
- 桌面装备页截图；
- 打开天赋详情后的截图与几何数据。

天赋详情弹层必须位于可视屏幕内、箭头对准节点，并且打开详情不能推动天赋树。

每次 UI 改动都应同时检查测试结论和截图，不要只看“JS 没报错”。

## 7. 下一阶段优先级 / Next priorities

### P0 — 2026-09-29 正式更新数据对账
`Depths of the Damned` 上线后逐职业核对节点位置、连线、名称、数值和描述，并确认四个基础职业最终树是否与预览不同。

### P1 — 官方简中翻译审计
重点检查武器完整型号、祝福、武器词条、珍品名称 / 主属性 / 词条。

### P1 — 真实用户路径回归
从空白 BD 开始完整走一次：选职业 → 30 点天赋 → 近战 → 远程 → 3 珍品 → 命名 / 备注 → 导出 / 分享。统计无意义重复点击，优先删掉重复步骤，而不是继续增加按钮。

### P2 — 武器视觉资产
仓库目前没有可靠的官方武器图片资产。**不要伪造图标或随便抓版权不明图片。** 只有在来源、许可和武器映射可靠后再加入武器图像浏览。

## 8. 当前部署 / Deployment

静态站点由 `main` 通过 GitHub Actions 自动部署到 GitHub Pages：

`https://a1615439938-collab.github.io/Darktide-BD/`

## 9. GitHub Actions 额度政策 / Scheduled automation policy

这是仓库级硬约束，后续接手的 AI、Codex 或其他自动化代理必须遵守：

1. **禁止在本仓库的 GitHub Actions 中新增任何定时触发器。** 不得在 `.github/workflows/*.yml` 中加入 `schedule:`、`cron:` 或等价的周期性 GitHub Actions 机制。
2. **不得恢复已删除的定时刷新。** `.github/workflows/refresh-trees.yml` 原来的每日 `cron: "15 3 * * *"` 已于 2026-09-27 按用户要求移除，不得自行恢复。
3. 需要刷新未来天赋树时，使用现有 `workflow_dispatch` 手动运行，或由明确的代码变更事件触发；不要为了“保持数据新鲜”而轮询。
4. 如果未来确实需要周期性后台任务，应优先迁移到不会消耗 GitHub Actions 额度的外部调度平台，并在实施前向用户说明；**不得由接手 AI 自行决定重新启用 GitHub cron。**
5. GitHub Pages、测试和审计可以继续按代码 push 等事件运行；本条政策针对的是“按时间自动周期运行”的 Actions。
6. 除非用户在新的明确指令中撤销本政策，否则把它视为长期约束，而不是临时的额度不足措施。

根目录 `AGENTS.md` 也记录了同一约束。修改任何 workflow 前必须先阅读该文件和本交接文档。

